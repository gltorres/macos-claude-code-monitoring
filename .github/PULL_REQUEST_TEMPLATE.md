## Summary

<!-- 1-3 bullets on what changed and why. -->

## Test plan

- [ ] `xcodebuild -project ClaudeMon.xcodeproj -scheme ClaudeMon -destination 'platform=macOS' test` passes locally
- [ ] For UI changes: `docs/manual-smoke-test.md` still passes (or note which steps are affected)
- [ ] `CHANGELOG.md` updated under `## [Unreleased]`
- [ ] No real `sessionKey`, account UUID, or email committed in fixtures or logs

## Screenshots (UI changes only)
